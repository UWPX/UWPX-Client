<UserControl
    x:Class="UWP_XMPP_Client.Controls.ChatMasterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:UWP_XMPP_Client.Controls"
    mc:Ignorable="d">

    <Grid Margin="0, 5, 0, 5" RightTapped="Grid_RightTapped">
        <Grid.Resources>
            <MenuFlyout x:Name="menuFlyout">
                <MenuFlyout.Items>
                    <MenuFlyoutSubItem Text="Presence subscription">
                        <MenuFlyoutItem x:Name="requestPresenceSubscription_mfo" Text="Request subscription" Visibility="Collapsed" Click="requestPresenceSubscription_mfo_Click"/>
                        <MenuFlyoutItem x:Name="cancelPresenceSubscription_mfo" Text="Cancel subscription" Visibility="Collapsed" Click="cancelPresenceSubscription_mfo_Click"/>
                        <MenuFlyoutItem x:Name="cancelPresenceSubscriptionRequest" Text="Cancel subscription request" Visibility="Collapsed" Click="cancelPresenceSubscriptionRequest_Click"/>
                        <MenuFlyoutItem x:Name="rejectPresenceSubscription_mfo" Text="Reject subscription" Visibility="Collapsed" Click="rejectPresenceSubscription_mfo_Click"/>
                    </MenuFlyoutSubItem>
                    <MenuFlyoutItem x:Name="removeFromRoster_mfo" Click="removeFromRoster_mfo_Click"/>
                    <MenuFlyoutItem x:Name="deleteChat_mfo" Text="Delete" Click="deleteChat_mfo_Click">
                    </MenuFlyoutItem>
                    <MenuFlyoutSeparator/>
                    <ToggleMenuFlyoutItem x:Name="mute_tmfo" Text="Mute" Click="mute_tmfo_Click"/>
                    <MenuFlyoutItem x:Name="block_tmfo" Text="Block"/>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem x:Name="showProfile_mfo" Text="Show profile"/>
                </MenuFlyout.Items>
            </MenuFlyout>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Rectangle x:Name="color_rcta" Grid.Column="0" Width="5" Fill="Transparent" Margin="-10, 0, 0, 0"/>
            <controls:AccountImageWithPresence x:Name="image_aciwp" Grid.Column="1" Margin="5, 0, 0, 0"/>
            <StackPanel Grid.Column="2" HorizontalAlignment="Stretch">
                <TextBlock x:Name="name_tblck" Margin="10, 0, 0, 0" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="lastChat_tblck" FontSize="12" Foreground="{ThemeResource SystemControlBackgroundBaseMediumBrush}" Margin="10, 0, 10, 0" TextTrimming="CharacterEllipsis" MaxLines="1"/>
                <TextBlock x:Name="presence_tblck" Grid.Column="0" Text="Subscription pending..." FontSize="10" Foreground="{StaticResource SystemControlBackgroundBaseMediumBrush}" Margin="10, 0, 0, 0" TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <Grid Grid.Column="3" HorizontalAlignment="Right">
                <Grid.RowDefinitions>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="16"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="inRooster_tbck" FontSize="12" Grid.Row="0" FontFamily="Segoe MDL2 Assets" Text="&#xE170;" Foreground="{StaticResource SystemControlBackgroundBaseMediumBrush}" HorizontalAlignment="Right" Margin="0, 0, 1, 0" VerticalAlignment="Center"/>
                <TextBlock x:Name="muted_tbck" FontSize="13" Grid.Row="1" FontFamily="Segoe MDL2 Assets" Text="&#xE74F;" Foreground="{StaticResource SystemControlBackgroundBaseMediumBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock x:Name="lastAction_tblck" Grid.Row="2" FontSize="10" Foreground="{StaticResource SystemControlBackgroundBaseMediumBrush}" TextTrimming="CharacterEllipsis" HorizontalAlignment="Right"/>
            </Grid>
        </Grid>
        <Grid Grid.Row="1" x:Name="accountAction_grid" Margin="0, 0, 0, 10" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="accountAction_tblck" Grid.ColumnSpan="2" Grid.Row="0" TextWrapping="WrapWholeWords" Margin="0, 0, 0, 5" Foreground="{ThemeResource SystemAccentColor}"/>
            <Button x:Name="accountActionAccept_btn" Grid.Column="0" Grid.Row="1" Content="Accept" Foreground="Green" HorizontalAlignment="Stretch" Margin="0, 0, 2, 0" Click="accountActionAccept_btn_Click"/>
            <Button x:Name="accountActionRefuse_btn" Grid.Column="1" Grid.Row="1" Content="Refuse" Foreground="Red" HorizontalAlignment="Stretch" Margin="2, 0, 0, 0" Click="accountActionRefuse_btn_Click"/>
        </Grid>
    </Grid>
</UserControl>
