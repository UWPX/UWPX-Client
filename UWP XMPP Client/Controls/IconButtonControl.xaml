<UserControl x:Class="UWP_XMPP_Client.Controls.IconButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Button Grid.Column="0"
            Click="Button_Click"
            IsEnabled="{x:Bind IsEnabled, Mode=OneWay}">
        <StackPanel Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center"
                       FontFamily="{x:Bind IconFontFamily, Mode=OneWay, FallbackValue='Segoe MDL2 Assets', TargetNullValue='Segoe MDL2 Assets'}"
                       Foreground="{x:Bind IconForeground, Mode=OneWay}"
                       Text="{x:Bind Icon, Mode=OneWay}"/>
            <TextBlock Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Text="{x:Bind Text, Mode=OneWay}"/>
        </StackPanel>
    </Button>
</UserControl>
