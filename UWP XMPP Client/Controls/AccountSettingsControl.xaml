<UserControl
    x:Class="UWP_XMPP_Client.Controls.AccountSettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:UWP_XMPP_Client.Controls"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="0, 0, 0, 10">
        <Grid Background="{ThemeResource SystemControlBackgroundAltMediumBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Rectangle x:Name="color_rcta" Grid.Column="0" Width="5" Fill="Transparent" Height="40"/>
                <Button x:Name="edit_btn" Grid.Column="1" FontFamily="Segoe MDL2 Assets" Content="&#xE1C2;" Height="40" Width="40" FontSize="20" Margin="10, 0, 10, 0" Click="edit_btn_Click" Foreground="{ThemeResource SystemAccentColor}"/>
                <controls:AccountImageWithPresenceControl x:Name="image_aciwp" Grid.Column="2"/>
                <Button x:Name="showSecurity_btn"
                        Grid.Column="3"
                        Content="&#xE785;"
                        Margin="5, 0, 10, 0"
                        HorizontalAlignment="Center"
                        Foreground="Red"
                        FontFamily="Segoe MDL2 Assets"
                        Click="showSecurity_btn_Click"/>
                <Grid Grid.Column="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="primaryInfo_tblck" Text="{x:Bind Account.user.userId, Mode=OneWay}" Grid.Row="0" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="secondaryInfo_tblck" Text="{x:Bind Account.user.domain, Mode=OneWay}" Grid.Row="1" FontSize="10" Foreground="{StaticResource SystemControlBackgroundBaseMediumBrush}" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="error_tblck" Grid.Row="2" FontSize="10" Foreground="Red" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" Visibility="Collapsed"/>
                </Grid>
                <ToggleSwitch x:Name="disableAccount_tggls" Grid.Column="5" OffContent="Disabled" OnContent="Enabled" IsOn="True" HorizontalAlignment="Right" Margin="10, 10, 0, 10" Toggled="disableAccount_tggls_Toggled"/>
            </Grid>
            <controls:SeperatorControl Grid.Row="1"/>
            <StackPanel x:Name="accountOptions_stckp" Visibility="Collapsed" Grid.Row="2" Margin="10">
                <controls:AccountControl x:Name="account_acc" Account="{x:Bind Account, Mode=TwoWay}"/>
                <Button x:Name="deleteAccount_btn" Margin="0, 20, 0, 0" Click="deleteAccount_btn_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE107;" Margin="0, -1, 5, 0" Foreground="Red" VerticalAlignment="Center"/>
                        <TextBlock Text="Delete Account" Foreground="Red"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
