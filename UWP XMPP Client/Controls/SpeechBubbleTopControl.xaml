<UserControl
    x:Class="UWP_XMPP_Client.Controls.SpeechBubbleTopControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Left" Padding="6">
        <Polygon Points="0,0 10,10 0,10" Fill="{ThemeResource SystemAccentColorDark1}" Stroke="Black" Margin="10,0,0,0" />

        <Border Background="{ThemeResource SystemAccentColorDark1}" BorderBrush="Black" BorderThickness="1" Padding="6" Margin="0,9,0,0" VerticalAlignment="Top" MinWidth="30">
            <StackPanel>
                <TextBlock Text="{x:Bind Text}" TextWrapping="WrapWholeWords"/>
                <TextBlock x:Name="date_tbx" FontSize="10" HorizontalAlignment="Right" Foreground="{StaticResource SystemControlBackgroundBaseMediumBrush}"/>
            </StackPanel>
        </Border>

        <Polygon Visibility="Visible" Points="0,0 10,10 0,10" Fill="{ThemeResource SystemAccentColorDark1}" Margin="10,0,0,0" />
    </Grid>
</UserControl>
