<ContentDialog
    x:Class="UWP_XMPP_Client.Dialogs.ConnectionSecurityInfoDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:UWP_XMPP_Client.Controls"
    mc:Ignorable="d"
    Title="Security info:"
    SecondaryButtonText="Close"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Pivot Grid.Row="0" Background="{ThemeResource SystemControlBackgroundAltMediumBrush}">
        <PivotItem Header="General">
            <ScrollViewer HorizontalScrollMode="Disabled">

            </ScrollViewer>
        </PivotItem>

        <PivotItem Header="Certificate">
            <controls:CertificateControl Cert="{x:Bind Cert, Mode=OneWay}"/>
        </PivotItem>
    </Pivot>
</ContentDialog>
